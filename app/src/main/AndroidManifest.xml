<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".mvvm.ui.auth.SignupActivity"></activity>
        <activity android:name=".mvvm.ui.auth.LoginActivity" />
        <activity android:name=".bottomsheetdialog.BottomSheetDialogActivity" />
        <activity android:name=".customalertdialog.CustomAlertDialogMainActivity" />
        <activity android:name=".base.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".scratch.ScratchActivity" />
        <activity android:name=".grammar.Main2Activity" />
        <activity android:name=".navigation.NaviMainActivity" />
    </application>

</manifest>